from pathlib import Path
import re

ROOT = Path('.')

def read(path): return (ROOT / path).read_text()
def write(path, text): (ROOT / path).write_text(text)
def rep(path, old, new, count=1):
    text = read(path)
    if old not in text:
        raise RuntimeError(f'pattern not found in {path}: {old[:120]!r}')
    text = text.replace(old, new, count)
    write(path, text)

def sub(path, pattern, repl, count=1, flags=0):
    text = read(path)
    out, n = re.subn(pattern, repl, text, count=count, flags=flags)
    if n != count:
        raise RuntimeError(f'regex count {n}!={count} in {path}: {pattern[:120]!r}')
    write(path, out)

# ---- Config types ---------------------------------------------------------
p='src/domain/config.ts'
rep(p, "  competitorDepositReactionSpeed?: number;\n", "  competitorDepositReactionSpeed?: number;\n  competitorTermDepositReactionSpeed?: number;\n  competitorConsumerLoanReactionSpeed?: number;\n")
rep(p, "export interface BehaviourParameters {\n", "export interface BoeFundingParameters {\n  strSpreadBps: number;\n  iltrSpreadBps: number;\n  strTenorMonths: number;\n  iltrTenorMonths: number;\n  levelAHaircut: number;\n}\n\nexport interface DepositRiskParameters {\n  uninsuredRunoffSensitivity: number;\n  largeDepositorRunoffSensitivity: number;\n}\n\nexport interface BehaviourParameters {\n")
rep(p, "  sharePriceModel?: SharePriceModelParameters;\n", "  sharePriceModel?: SharePriceModelParameters;\n  boeFunding?: BoeFundingParameters;\n  depositRisk?: DepositRiskParameters;\n")

# ---- Base config products / behaviours ----------------------------------
p='src/config/baseConfig.ts'
consumer_params="""  [AssetProductType.ConsumerLoans]: {\n    productType: AssetProductType.ConsumerLoans,\n    riskWeight: 0.75,\n    baseDefaultRate: 0.025,\n    lossGivenDefault: 0.65,\n    volumeElasticityToRate: -0.55,\n    adverseSelectionRatePremiumThreshold: 0.01,\n    adverseSelectionPdSlope: 10,\n    adverseSelectionMaxMultiplier: 2.8,\n    loan: {\n      defaultTermMonths: 48,\n      maxTermMonths: 84,\n      initialSeasoningEnabled: true,\n      initialCouponDispersionBps: 120,\n      initialPdMultiplierRange: { min: 0.8, max: 1.5 },\n      initialLgdMultiplierRange: { min: 0.9, max: 1.1 },\n      initialMinBucketOutstanding: 10e6,\n    },\n  },\n"""
rep(p, "  [AssetProductType.CorporateLoans]: {\n", consumer_params + "  [AssetProductType.CorporateLoans]: {\n")
term_params="""  [LiabilityProductType.RetailTermDeposits]: {\n    productType: LiabilityProductType.RetailTermDeposits, riskWeight: 0, baseDefaultRate: 0, lossGivenDefault: 0, volumeElasticityToRate: 0.7,\n  },\n"""
rep(p, "  [LiabilityProductType.CorporateOperatingDeposits]: {\n", term_params + "  [LiabilityProductType.CorporateOperatingDeposits]: {\n")
new_funding_params="""  [LiabilityProductType.BankOfEnglandFunding]: { productType: LiabilityProductType.BankOfEnglandFunding, riskWeight: 0, baseDefaultRate: 0, lossGivenDefault: 0, volumeElasticityToRate: 0 },\n  [LiabilityProductType.Tier2Debt]: { productType: LiabilityProductType.Tier2Debt, riskWeight: 0, baseDefaultRate: 0, lossGivenDefault: 0, volumeElasticityToRate: 0 },\n"""
rep(p, "  [LiabilityProductType.RepurchaseAgreements]: {\n", new_funding_params + "  [LiabilityProductType.RepurchaseAgreements]: {\n")
consumer_liq="""  [AssetProductType.ConsumerLoans]: {\n    productType: AssetProductType.ConsumerLoans, hqlaLevel: HQLALevel.None, lcrInflowRate: 0.05, nsfrRsfFactor: .85,\n  },\n"""
# second CorporateLoans occurrence is in liquidityTags
text=read(p); idx=text.find("const liquidityTags")
pos=text.find("  [AssetProductType.CorporateLoans]: {\n", idx)
if pos<0: raise RuntimeError('liquidity corporate marker missing')
text=text[:pos]+consumer_liq+text[pos:]; write(p,text)
term_liq="""  [LiabilityProductType.RetailTermDeposits]: {\n    productType: LiabilityProductType.RetailTermDeposits, hqlaLevel: HQLALevel.None, lcrOutflowRate: 0, nsfrAsfFactor: .95,\n  },\n"""
text=read(p); idx=text.find("const liquidityTags"); pos=text.find("  [LiabilityProductType.CorporateOperatingDeposits]: {\n", idx)
if pos<0: raise RuntimeError('liquidity term marker missing')
text=text[:pos]+term_liq+text[pos:]; write(p,text)
fund_liq="""  [LiabilityProductType.BankOfEnglandFunding]: { productType: LiabilityProductType.BankOfEnglandFunding, hqlaLevel: HQLALevel.None, lcrOutflowRate: 0, nsfrAsfFactor: .5 },\n  [LiabilityProductType.Tier2Debt]: { productType: LiabilityProductType.Tier2Debt, hqlaLevel: HQLALevel.None, lcrOutflowRate: 0, nsfrAsfFactor: 1 },\n"""
text=read(p); idx=text.find("const liquidityTags"); pos=text.find("  [LiabilityProductType.RepurchaseAgreements]: {\n", idx)
if pos<0: raise RuntimeError('liquidity funding marker missing')
text=text[:pos]+fund_liq+text[pos:]; write(p,text)
rep(p, "  competitorDepositReactionSpeed: 0.035,\n", "  competitorDepositReactionSpeed: 0.035,\n  competitorTermDepositReactionSpeed: 0.025,\n  competitorConsumerLoanReactionSpeed: 0.025,\n")
term_beh="""    [LiabilityProductType.RetailTermDeposits]: {\n      baselineGrowthMonthly: 0.008, baseChurnMonthly: 0, policyRateBeta: 0, competitorSensitivity: 0.75,\n      passThroughLag: 0.85, underpricingConvexity: 18, underpricingDurationSensitivity: 0.04,\n      franchiseDecayRate: 0.25, franchiseRecoveryRate: 0.04, reacquisitionDrag: 0.2,\n      stabilityDecayRate: 0.1, stabilityRecoveryRate: 0.08, mixMigrationRate: 0, mixMigrationDurationSensitivity: 0,\n    },\n"""
text=read(p); idx=text.find("  depositByProduct:"); pos=text.find("    [LiabilityProductType.CorporateOperatingDeposits]: {\n", idx)
if pos<0: raise RuntimeError('deposit behaviour marker missing')
text=text[:pos]+term_beh+text[pos:]; write(p,text)
consumer_pipe="""    [AssetProductType.ConsumerLoans]: {\n      baseDemandRateMonthly: 0.035, pricingSensitivity: 18, macroSensitivity: 1.6, baseApprovalRate: 0.62,\n      underwritingSensitivity: 0.7, drawdownRateMonthly: 0.7, cancellationRateMonthly: 0.08,\n      referenceMarketSize: 220e9, referenceBankShare: 0.0032, neutralGdpGrowthMonthly: 0.0015,\n      neutralUnemploymentRate: 0.045, neutralBorrowerRate: 0.105, neutralCreditSpread: 0.012,\n      gdpMarketSensitivity: 45, unemploymentMarketSensitivity: 6, borrowingCostMarketSensitivity: 2.5,\n      creditSpreadMarketSensitivity: 5, minAddressableMarketMultiplier: 0.4, maxAddressableMarketMultiplier: 1.5,\n    },\n"""
text=read(p); idx=text.find("  loanPipelineByProduct:"); pos=text.find("    [AssetProductType.CorporateLoans]: {\n", idx)
if pos<0: raise RuntimeError('consumer pipeline marker missing')
text=text[:pos]+consumer_pipe+text[pos:]; write(p,text)
consumer_aff="""      [AssetProductType.ConsumerLoans]: {\n        baselineDriftMonthly: 0.004, couponGapSensitivity: 2.5, policyRateSensitivity: 0.8,\n        unemploymentSensitivity: 5.0, gdpContractionSensitivity: 18, recoverySpeedMonthly: 0.05,\n        pdStressSlope: 1.5, minIndex: 0.7, maxIndex: 3.5, resetShareOnRenewal: 0.3,\n      },\n"""
text=read(p); idx=text.find("    affordabilityByProduct:"); pos=text.find("      [AssetProductType.CorporateLoans]: {\n", idx)
if pos<0: raise RuntimeError('consumer affordability marker missing')
text=text[:pos]+consumer_aff+text[pos:]; write(p,text)
consumer_refi="""      [AssetProductType.ConsumerLoans]: {\n        minSeasoningMonths: 6, basePrepayRateMonthly: 0.004, incentiveSensitivity: 0.35, riskSelectivity: 0.35,\n        minPrepayRateMonthly: 0.001, maxPrepayRateMonthly: 0.02,\n      },\n"""
text=read(p); idx=text.find("    refinanceByProduct:"); pos=text.find("      [AssetProductType.CorporateLoans]: {\n", idx)
if pos<0: raise RuntimeError('consumer refi marker missing')
text=text[:pos]+consumer_refi+text[pos:]; write(p,text)
rep(p, "  sharePriceModel: {\n", "  boeFunding: { strSpreadBps: 0, iltrSpreadBps: 3, strTenorMonths: 1, iltrTenorMonths: 6, levelAHaircut: 0.03 },\n  depositRisk: { uninsuredRunoffSensitivity: 0.35, largeDepositorRunoffSensitivity: 0.8 },\n  sharePriceModel: {\n")

# ---- Opening small retail bank ------------------------------------------
p='src/config/initialState.ts'
# Replace balance-sheet object wholesale.
sub(p, r"const balanceSheet: BalanceSheet = \{\n  items: \[.*?\n  \],\n\};", """const balanceSheet: BalanceSheet = {\n  items: [\n    makeItem(BalanceSheetSide.Asset, AssetProductType.CashReserves, 'Cash & Reserves', 1.5e9, 0.031, MaturityBucket.Overnight),\n    makeItem(BalanceSheetSide.Asset, AssetProductType.Gilts, 'Gilts / Liquidity Portfolio', 2.5e9, 0.041, MaturityBucket.GreaterThan5Y),\n    makeItem(BalanceSheetSide.Asset, AssetProductType.Mortgages, 'Residential Mortgages', 7.0e9, 0.050, MaturityBucket.GreaterThan5Y),\n    makeItem(BalanceSheetSide.Asset, AssetProductType.ConsumerLoans, 'Personal Loans & Revolving Credit', 0.7e9, 0.105, MaturityBucket.OneToThreeY),\n    makeItem(BalanceSheetSide.Asset, AssetProductType.CorporateLoans, 'SME & Business Lending', 2.261e9, 0.068, MaturityBucket.ThreeToFiveY),\n    makeItem(BalanceSheetSide.Liability, LiabilityProductType.RetailTransactionalDeposits, 'Retail Current Accounts', 2.0e9, 0.005, MaturityBucket.LessThan1Y),\n    makeItem(BalanceSheetSide.Liability, LiabilityProductType.RetailSavingsDeposits, 'Instant-Access Savings', 5.0e9, 0.0215, MaturityBucket.LessThan1Y),\n    makeItem(BalanceSheetSide.Liability, LiabilityProductType.RetailTermDeposits, 'Fixed-Term Savings', 1.5e9, 0.038, MaturityBucket.OneToThreeY),\n    makeItem(BalanceSheetSide.Liability, LiabilityProductType.CorporateOperatingDeposits, 'SME / Business Operating Deposits', 3.0e9, 0.0205, MaturityBucket.LessThan1Y),\n    makeItem(BalanceSheetSide.Liability, LiabilityProductType.CorporateNonOperatingDeposits, 'Other Business Deposits', 0.3e9, 0.030, MaturityBucket.LessThan1Y),\n    makeItem(BalanceSheetSide.Liability, LiabilityProductType.WholesaleFundingLT, 'Long-Term Debt', 1.2e9, 0.053, MaturityBucket.GreaterThan5Y),\n  ],\n};""", flags=re.S)
rep(p, "  at1: 0.136e9,\n", "  at1: 0.136e9,\n  tier2: 0,\n")
rep(p, "  mortgageSpread: 0.013,\n  corporateLoanSpread: 0.021,\n", "  mortgageSpread: 0.013,\n  consumerLoanSpread: 0.057,\n  corporateLoanSpread: 0.021,\n")
rep(p, "  competitorRetailDepositRate: 0.019,\n  competitorMortgageRate: 0.049,\n", "  competitorRetailDepositRate: 0.019,\n  competitorTermDepositRate: 0.038,\n  competitorMortgageRate: 0.049,\n  competitorConsumerLoanRate: 0.105,\n")
rep(p, "    [LiabilityProductType.RetailSavingsDeposits]: 0.0215,\n", "    [LiabilityProductType.RetailSavingsDeposits]: 0.0215,\n    [LiabilityProductType.RetailTermDeposits]: 0.038,\n")
rep(p, "    [LiabilityProductType.RetailSavingsDeposits]: 0,\n", "    [LiabilityProductType.RetailSavingsDeposits]: 0,\n    [LiabilityProductType.RetailTermDeposits]: 0,\n")
rep(p, "    [LiabilityProductType.RetailSavingsDeposits]: 1.0925,\n", "    [LiabilityProductType.RetailSavingsDeposits]: 1.0925,\n    [LiabilityProductType.RetailTermDeposits]: 1.1,\n")
rep(p, "    [AssetProductType.Mortgages]: 0,\n    [AssetProductType.CorporateLoans]: 0,\n", "    [AssetProductType.Mortgages]: 0,\n    [AssetProductType.ConsumerLoans]: 0.35,\n    [AssetProductType.CorporateLoans]: 0.25,\n")
rep(p, "  capitalPolicy: {\n", "  mortgagePolicy: { maxLtv: 0.85, fixedPeriodMonths: 24 },\n  treasuryPolicy: { giltShareOfHqla: 0.625, giltDurationYears: 5 },\n  termDepositTenorMonths: 12,\n  insuredRetailDepositShare: 0.9,\n  largeDepositorShare: 0.04,\n  capitalPolicy: {\n")
# loan pipelines/workouts
rep(p, "    [AssetProductType.CorporateLoans]: {\n      demandNotional: 0,\n", "    [AssetProductType.ConsumerLoans]: { demandNotional: 0, approvedNotional: 0, committedNotional: 0 },\n    [AssetProductType.CorporateLoans]: {\n      demandNotional: 0,\n")
rep(p, "    [AssetProductType.Mortgages]: [],\n    [AssetProductType.CorporateLoans]: [],\n", "    [AssetProductType.Mortgages]: [],\n    [AssetProductType.ConsumerLoans]: [],\n    [AssetProductType.CorporateLoans]: [],\n")
# funding ladders: replace object body
sub(p, r"  fundingLadders: \{\n.*?\n  \},\n  status,", """  fundingLadders: {\n    [LiabilityProductType.RetailTermDeposits]: Array.from({ length: 12 }, (_, i) => ({ tenorMonths: 12, monthsToMaturity: i + 1, notional: 125e6, rate: 0.038 })),\n    [LiabilityProductType.WholesaleFundingLT]: [\n      { tenorMonths: 24, monthsToMaturity: 24, notional: 400e6, rate: 0.053 },\n      { tenorMonths: 36, monthsToMaturity: 36, notional: 400e6, rate: 0.053 },\n      { tenorMonths: 60, monthsToMaturity: 60, notional: 400e6, rate: 0.053 },\n    ],\n    [LiabilityProductType.BankOfEnglandFunding]: [],\n    [LiabilityProductType.Tier2Debt]: [],\n  },\n  status,""", flags=re.S)
# remove opening repo encumbrance block
sub(p, r"\n// Opening repo stock is secured on gilts;.*?openingGilts\.encumbrance = .*?;\n", "\n", flags=re.S)
rep(p, "seedLoanCohorts(AssetProductType.Mortgages);\nseedLoanCohorts(AssetProductType.CorporateLoans);", "seedLoanCohorts(AssetProductType.Mortgages);\nseedLoanCohorts(AssetProductType.ConsumerLoans);\nseedLoanCohorts(AssetProductType.CorporateLoans);")

# ---- Loan cohort engine --------------------------------------------------
p='src/engine/loanCohorts.ts'
rep(p, "  'retailMortgage',\n  'commercialRealEstate',\n", "  'retailMortgage',\n  'consumer',\n  'commercialRealEstate',\n")
rep(p, "const fallbackSectorForProduct = (productType: ProductType): LoanSector =>\n  productType === AssetProductType.Mortgages ? 'retailMortgage' : 'largeCorporate';", "const fallbackSectorForProduct = (productType: ProductType): LoanSector =>\n  productType === AssetProductType.Mortgages ? 'retailMortgage' : productType === AssetProductType.ConsumerLoans ? 'consumer' : 'sme';")
# enrich new cohorts by replacing object fields in upsert routine
rep(p, "    annualPd: args.annualPd,\n    lgd: args.lgd,\n", "    annualPd: args.productType === AssetProductType.Mortgages ? args.annualPd * (1 + Math.max(0, (args.state.behaviour.mortgagePolicy?.maxLtv ?? .85) - .75) * 1.6) : args.annualPd,\n    lgd: args.productType === AssetProductType.Mortgages ? clamp(args.lgd + Math.max(0, (args.state.behaviour.mortgagePolicy?.maxLtv ?? .85) - .75) * .8, 0, .95) : args.lgd,\n    ltv: args.productType === AssetProductType.Mortgages ? (args.state.behaviour.mortgagePolicy?.maxLtv ?? .85) : undefined,\n    fixedPeriodMonths: args.productType === AssetProductType.Mortgages ? (args.state.behaviour.mortgagePolicy?.fixedPeriodMonths ?? 24) : undefined,\n", count=1)

# ---- Prudential liquidity ------------------------------------------------
p='src/engine/prudential.ts'
rep(p, "committedExposure(s, AssetProductType.Mortgages) * .05 + committedExposure(s, AssetProductType.CorporateLoans) * .1", "committedExposure(s, AssetProductType.Mortgages) * .05 + committedExposure(s, AssetProductType.ConsumerLoans) * .1 + committedExposure(s, AssetProductType.CorporateLoans) * .1")
rep(p, "  if (p === LiabilityProductType.WholesaleFundingST || p === LiabilityProductType.WholesaleFundingLT) {", "  if ([LiabilityProductType.WholesaleFundingST, LiabilityProductType.WholesaleFundingLT, LiabilityProductType.RetailTermDeposits, LiabilityProductType.BankOfEnglandFunding, LiabilityProductType.Tier2Debt].includes(p as LiabilityProductType)) {")
# term deposits get outflow only at near maturity; existing generic ladder logic handles that. preserve 95% ASF below 12m for retail term.
rep(p, "      asf = buckets.reduce((sum, f) => sum + f.notional * (f.monthsToMaturity >= 12 ? 1 : f.monthsToMaturity >= 6 ? .5 : 0), 0);", "      asf = buckets.reduce((sum, f) => {\n        if (p === LiabilityProductType.RetailTermDeposits) return sum + f.notional * (f.monthsToMaturity >= 12 ? 1 : .95);\n        return sum + f.notional * (f.monthsToMaturity >= 12 ? 1 : f.monthsToMaturity >= 6 ? .5 : 0);\n      }, 0);")

# ---- Market model: evolve term savings + consumer benchmark --------------
p='src/engine/ukMarketModel.ts'
rep(p, "    market.mortgageSpread = clamp(\n", "    market.consumerLoanSpread = clamp(meanRevert(market.consumerLoanSpread, 0.045 + 1.2 * creditSpread, pass) + rng.normal() * noise, 0.02, 0.18);\n    market.mortgageSpread = clamp(\n")
rep(p, "    if (market.competitorCorporateDepositRate !== undefined) {", "    const termDepositTarget = Math.max(0, bankRateClamped - 0.006);\n    market.competitorTermDepositRate = clamp(meanRevert(market.competitorTermDepositRate, termDepositTarget, 0.25) + rng.normal() * noise, 0, Math.max(bankRateClamped + .01, termDepositTarget));\n    if (market.competitorCorporateDepositRate !== undefined) {")
rep(p, "    market.competitorMortgageRate = clamp(\n", "    market.competitorConsumerLoanRate = clamp(meanRevert(market.competitorConsumerLoanRate, bankRateClamped + market.consumerLoanSpread, 0.18) + rng.normal() * noise, 0.03, 0.35);\n    market.competitorMortgageRate = clamp(\n")

# ---- Metrics: Tier 2, deposit concentration, structural IRRBB ------------
p='src/engine/metrics.ts'
rep(p, "const FUNDING_PRODUCTS: Array<\n  LiabilityProductType.WholesaleFundingST | LiabilityProductType.WholesaleFundingLT\n> = [LiabilityProductType.WholesaleFundingST, LiabilityProductType.WholesaleFundingLT];", "const FUNDING_PRODUCTS: ProductType[] = [LiabilityProductType.RetailTermDeposits, LiabilityProductType.WholesaleFundingST, LiabilityProductType.WholesaleFundingLT, LiabilityProductType.BankOfEnglandFunding, LiabilityProductType.Tier2Debt];")
# deposit quality: add concentration effects just before return
rep(p, "  return clamp(weighted / total, 0.4, 1.1);\n};", "  const retail = deposits.filter(i => PRODUCT_META[i.productType]?.behaviour?.depositSegment === 'retail').reduce((s,i)=>s+Math.max(0,i.balance),0);\n  const insured = clamp(state.behaviour.insuredRetailDepositShare ?? .9, 0, 1);\n  const large = clamp(state.behaviour.largeDepositorShare ?? .04, 0, .5);\n  const uninsuredPenalty = retail > 0 ? (1-insured) * .18 : 0;\n  const concentrationPenalty = large * .35;\n  return clamp(weighted / total - uninsuredPenalty - concentrationPenalty, 0.4, 1.1);\n};")
# replace entire IRRBB function with product-specific approximation
sub(p, r"const computeIrrbbSensitivities = \(.*?\n\};\n\nconst CET1_GEOGRAPHY_FALLBACKS", """const computeIrrbbSensitivities = (\n  state: BankState,\n  config: SimulationConfig\n): { niiSensitivity100bp: number; eveSensitivity100bp: number } => {\n  const duration = (item: BalanceSheetItem): number => {\n    switch (item.productType) {\n      case AssetProductType.CashReserves: return 0.05;\n      case AssetProductType.Gilts: return Math.max(.25, state.behaviour.treasuryPolicy?.giltDurationYears ?? item.security?.effectiveDurationYears ?? 5);\n      case AssetProductType.Mortgages: return Math.max(.5, Math.min(5, (state.behaviour.mortgagePolicy?.fixedPeriodMonths ?? 24) / 12 * .85));\n      case AssetProductType.ConsumerLoans: return 1.25;\n      case AssetProductType.CorporateLoans: return 1.75;\n      case LiabilityProductType.RetailTransactionalDeposits: return .35;\n      case LiabilityProductType.RetailSavingsDeposits: return .45;\n      case LiabilityProductType.RetailTermDeposits: {\n        const b=state.fundingLadders?.[item.productType]??[]; const n=b.reduce((s,x)=>s+x.notional,0); return n>0?b.reduce((s,x)=>s+x.notional*Math.max(.08,x.monthsToMaturity/12),0)/n:1;\n      }\n      case LiabilityProductType.CorporateOperatingDeposits: return .25;\n      case LiabilityProductType.CorporateNonOperatingDeposits: return .15;\n      case LiabilityProductType.WholesaleFundingLT: case LiabilityProductType.BankOfEnglandFunding: case LiabilityProductType.Tier2Debt: {\n        const b=state.fundingLadders?.[item.productType]??[]; const n=b.reduce((s,x)=>s+x.notional,0); return n>0?b.reduce((s,x)=>s+x.notional*Math.max(.08,x.monthsToMaturity/12),0)/n:1;\n      }\n      default: return item.side===BalanceSheetSide.Asset ? (config.behaviour.irrbb?.baseAssetDurationYears ?? 3) : (config.behaviour.irrbb?.baseLiabilityDurationYears ?? 1.5);\n    }\n  };\n  let assetDv=0, liabilityDv=0, assetReprice=0, liabilityReprice=0;\n  for (const item of state.financial.balanceSheet.items) {\n    const b=Math.max(0,item.balance); if(!b) continue; const d=duration(item); const repr=clamp(1/(1+d),.04,1);\n    if(item.side===BalanceSheetSide.Asset){assetDv+=b*d;assetReprice+=b*repr;} else {liabilityDv+=b*d;liabilityReprice+=b*repr;}\n  }\n  const hedgeNiiOffset=(state.financial.hedges??[]).reduce((s,h)=>s+(h.direction==='payFixedReceiveFloat'?1:-1)*h.notional,0);\n  const hedgeDurationOffset=(state.financial.hedges??[]).reduce((s,h)=>s+(h.direction==='payFixedReceiveFloat'?-1:1)*h.notional*Math.max(.25,Math.min(5,h.monthsRemaining/12)),0);\n  return { niiSensitivity100bp:(assetReprice-liabilityReprice+hedgeNiiOffset)*.01, eveSensitivity100bp:-(assetDv-liabilityDv+hedgeDurationOffset)*.01 };\n};\n\nconst CET1_GEOGRAPHY_FALLBACKS""", flags=re.S)
rep(p, "  productType === AssetProductType.Mortgages ? 'retailMortgage' : 'largeCorporate';", "  productType === AssetProductType.Mortgages ? 'retailMortgage' : productType === AssetProductType.ConsumerLoans ? 'consumer' : 'sme';")
# total capital with tier2 and report deposit metrics
rep(p, "  const adjustedCet1 = state.financial.capital.cet1 + state.financial.capital.accumulatedOCI * fvociInclusionRate;", "  const adjustedCet1 = state.financial.capital.cet1 + state.financial.capital.accumulatedOCI * fvociInclusionRate;\n  const tier2 = Math.max(0, state.financial.capital.tier2 ?? 0);")
rep(p, "  const ownFundsCet1Floor = rwa > 0 ? Math.max(minima.cet1, minima.tier1 - state.financial.capital.at1 / rwa, minima.total - state.financial.capital.at1 / rwa) : minima.cet1;", "  const ownFundsCet1Floor = rwa > 0 ? Math.max(minima.cet1, minima.tier1 - state.financial.capital.at1 / rwa, minima.total - (state.financial.capital.at1 + tier2) / rwa) : minima.cet1;")
rep(p, "    totalCapitalRequirement: minima.total + computeCet1Requirement(config.riskLimits) - config.riskLimits.minCet1Ratio,", "    totalCapitalRequirement: minima.total + computeCet1Requirement(config.riskLimits) - config.riskLimits.minCet1Ratio,")
rep(p, "    totalCapitalRatio: rwa > 0 ? (adjustedCet1 + state.financial.capital.at1) / rwa : Infinity,", "    totalCapitalRatio: rwa > 0 ? (adjustedCet1 + state.financial.capital.at1 + tier2) / rwa : Infinity,")
rep(p, "    depositQualityIndex,\n", "    depositQualityIndex,\n    insuredRetailDepositShare: clamp(state.behaviour.insuredRetailDepositShare ?? .9,0,1),\n    largeDepositorShare: clamp(state.behaviour.largeDepositorShare ?? .04,0,.5),\n    termDepositShare: (()=>{const deps=state.financial.balanceSheet.items.filter(i=>PRODUCT_META[i.productType]?.behaviour?.isCustomerDeposit).reduce((s,i)=>s+Math.max(0,i.balance),0); const term=state.financial.balanceSheet.items.find(i=>i.productType===LiabilityProductType.RetailTermDeposits)?.balance??0; return deps>0?term/deps:0;})(),\n")

# ---- Simulation engine actions / funding / demand ------------------------
p='src/engine/simulation.ts'
rep(p, "  IssueEquityAction,\n  EnterRepoAction,\n", "  IssueEquityAction,\n  IssueTier2Action,\n  DrawBoeFundingAction,\n  SetMortgagePolicyAction,\n  SetTreasuryPolicyAction,\n  SetTermDepositPolicyAction,\n  EnterRepoAction,\n")
# add handlers before enterRepo
rep(p, "  enterRepo: (action: EnterRepoAction, ctx) => {", "  issueTier2: (action: IssueTier2Action, ctx) => { applyIssueTier2(ctx.state,ctx.config,action.amount,action.maturityMonths,ctx.events); },\n  drawBoeFunding: (action: DrawBoeFundingAction, ctx) => { applyBoeFunding(ctx.state,ctx.config,action.facility,action.amount,ctx.events); },\n  setMortgagePolicy: (action: SetMortgagePolicyAction, ctx) => { ctx.state.behaviour.mortgagePolicy={maxLtv:clamp(action.maxLtv,.5,.95),fixedPeriodMonths:Math.max(12,Math.round(action.fixedPeriodMonths))}; ctx.events.push(createEvent('info',`Mortgage policy: max LTV ${(ctx.state.behaviour.mortgagePolicy.maxLtv*100).toFixed(0)}%, fixed ${ctx.state.behaviour.mortgagePolicy.fixedPeriodMonths}m`)); },\n  setTreasuryPolicy: (action: SetTreasuryPolicyAction, ctx) => { ctx.state.behaviour.treasuryPolicy={giltShareOfHqla:clamp(action.giltShareOfHqla,0,1),giltDurationYears:clamp(action.giltDurationYears,.25,15)}; applyTreasuryPolicy(ctx.state,ctx.config,ctx.events); },\n  setTermDepositPolicy: (action: SetTermDepositPolicyAction, ctx) => { ctx.state.behaviour.termDepositTenorMonths=Math.max(6,Math.round(action.tenorMonths)); },\n  enterRepo: (action: EnterRepoAction, ctx) => {")
# insert helper mechanics before applyEnterHedge
marker="const applyEnterHedge = (\n"
helpers="""const genericFundingBuckets = (state: BankState, productType: ProductType): FundingMaturityBucket[] => {\n  state.fundingLadders ??= {};\n  return state.fundingLadders[productType] ?? (state.fundingLadders[productType] = []);\n};\n\nconst applyIssueTier2 = (state: BankState, config: SimulationConfig, amount: number, maturityMonths: number | undefined, events: SimulationEvent[]): void => {\n  const issued=Math.max(0,amount); if(!issued)return; const cash=findItem(state.financial.balanceSheet,AssetProductType.CashReserves); if(!cash)return;\n  const tenor=Math.max(60,Math.round(maturityMonths??60)); const rate=Math.max(0,state.market.riskFreeLong+state.market.seniorDebtSpread+.015);\n  const line=ensureLineItem(state,BalanceSheetSide.Liability,LiabilityProductType.Tier2Debt,'Tier 2 Subordinated Debt',rate,config);\n  genericFundingBuckets(state,LiabilityProductType.Tier2Debt).push({tenorMonths:tenor,monthsToMaturity:tenor,notional:issued,rate});\n  line.balance+=issued; line.interestRate=blendRate(Math.max(0,line.balance-issued),line.interestRate,issued,rate); state.financial.capital.tier2=(state.financial.capital.tier2??0)+issued; cash.balance+=issued;\n  events.push(createEvent('info',`Issued Tier 2 ${issued.toFixed(2)} at ${(rate*100).toFixed(2)}% for ${tenor}m`,['capital','funding']));\n};\n\nconst applyBoeFunding = (state: BankState, config: SimulationConfig, facility: 'STR'|'ILTR', amount: number, events: SimulationEvent[]): void => {\n  const requested=Math.max(0,amount); if(!requested)return; const cash=findItem(state.financial.balanceSheet,AssetProductType.CashReserves); const gilts=findItem(state.financial.balanceSheet,AssetProductType.Gilts); if(!cash||!gilts)return;\n  const p=config.behaviour.boeFunding; const haircut=clamp(Math.max(p?.levelAHaircut??.03,state.market.giltRepoHaircut),0,.25); const available=Math.max(0,gilts.balance-(gilts.encumbrance?.encumberedAmount??0)); const executable=Math.min(requested,available*(1-haircut));\n  if(executable<=0){events.push(createEvent('warning','Bank of England drawing rejected: no unencumbered eligible gilt collateral.'));return;}\n  const tenor=facility==='STR'?(p?.strTenorMonths??1):(p?.iltrTenorMonths??6); const spread=(facility==='STR'?(p?.strSpreadBps??0):(p?.iltrSpreadBps??3))/10000; const rate=Math.max(0,state.market.baseRate+spread);\n  const line=ensureLineItem(state,BalanceSheetSide.Liability,LiabilityProductType.BankOfEnglandFunding,'Bank of England Secured Funding',rate,config); genericFundingBuckets(state,LiabilityProductType.BankOfEnglandFunding).push({tenorMonths:tenor,monthsToMaturity:tenor,notional:executable,rate}); line.balance+=executable; line.interestRate=blendRate(Math.max(0,line.balance-executable),line.interestRate,executable,rate); cash.balance+=executable;\n  gilts.encumbrance??={encumberedAmount:0}; gilts.encumbrance.encumberedAmount=Math.min(gilts.balance,(gilts.encumbrance.encumberedAmount??0)+executable/(1-haircut)); gilts.encumbrance.remainingMonths=Math.max(gilts.encumbrance.remainingMonths??0,tenor);\n  events.push(createEvent('info',`${facility} drawing ${executable.toFixed(2)} at ${(rate*100).toFixed(2)}%, secured on gilts`,['funding','liquidity']));\n};\n\nconst applyTreasuryPolicy = (state: BankState, config: SimulationConfig, events: SimulationEvent[]): void => {\n  const policy=state.behaviour.treasuryPolicy; if(!policy)return; const cash=findItem(state.financial.balanceSheet,AssetProductType.CashReserves); const gilts=findItem(state.financial.balanceSheet,AssetProductType.Gilts); if(!cash||!gilts)return;\n  const total=Math.max(0,cash.balance)+Math.max(0,gilts.balance); const target=total*clamp(policy.giltShareOfHqla,0,1); const delta=target-gilts.balance; if(Math.abs(delta)>1e4) applyBuySellAsset(state,config,AssetProductType.Gilts,delta,events);\n  if(gilts.security) gilts.security.effectiveDurationYears=clamp(policy.giltDurationYears,.25,15);\n};\n\nconst stepContractualRetailFunding = (state: BankState, config: SimulationConfig, dtMonths: number, events: SimulationEvent[]): void => {\n  const products=[LiabilityProductType.RetailTermDeposits,LiabilityProductType.BankOfEnglandFunding,LiabilityProductType.Tier2Debt] as ProductType[];\n  for(const productType of products){ const buckets=genericFundingBuckets(state,productType); if(!buckets.length)continue; let matured=0, releasedCollateral=0; const before=buckets.reduce((s,b)=>s+b.notional,0); const keep:FundingMaturityBucket[]=[];\n    for(const b of buckets){const m=b.monthsToMaturity-dtMonths;if(m<=0)matured+=Math.max(0,b.notional);else keep.push({...b,monthsToMaturity:m});} state.fundingLadders[productType]=keep; if(matured<=0)continue; const paid=applyCashOutflowOrFail(state,matured,events); const line=findItem(state.financial.balanceSheet,productType); if(line)line.balance=Math.max(0,(line.balance??before)-paid);\n    if(productType===LiabilityProductType.Tier2Debt) state.financial.capital.tier2=Math.max(0,(state.financial.capital.tier2??0)-paid);\n    if(productType===LiabilityProductType.BankOfEnglandFunding){const gilts=findItem(state.financial.balanceSheet,AssetProductType.Gilts);if(gilts?.encumbrance&&before>0){releasedCollateral=(gilts.encumbrance.encumberedAmount??0)*Math.min(1,paid/before);gilts.encumbrance.encumberedAmount=Math.max(0,(gilts.encumbrance.encumberedAmount??0)-releasedCollateral);}}\n    events.push(createEvent('info',`${PRODUCT_META[productType]?.label??productType} matured ${paid.toFixed(2)}`,['funding']));\n  }\n};\n\n"""
rep(p, marker, helpers+marker)
# include term deposit in retail products
rep(p, "  LiabilityProductType.RetailSavingsDeposits,\n];", "  LiabilityProductType.RetailSavingsDeposits,\n  LiabilityProductType.RetailTermDeposits,\n];")
# competitor special case term + consumer benchmark
rep(p, "      const competitor =\n        meta.behaviour.depositSegment === 'corporate'\n          ? state.market.competitorCorporateDepositRate ?? state.market.competitorRetailDepositRate\n          : state.market.competitorRetailDepositRate;", "      const competitor = meta.behaviour.isTermDeposit ? state.market.competitorTermDepositRate : meta.behaviour.depositSegment === 'corporate' ? state.market.competitorCorporateDepositRate ?? state.market.competitorRetailDepositRate : state.market.competitorRetailDepositRate;")
# don't permit contractual deposit runoff; positive acquisition becomes ladder buckets
rep(p, "      const delta = item.balance * growth;\n", "      const delta = item.balance * growth;\n", count=1)
# patch later generic deposit balance mutation by detecting exact common statement
rep(p, "      item.balance = Math.max(0, item.balance + delta);\n      adjustCashOrFail(state, delta, events);", "      const effectiveDelta = meta.behaviour.isTermDeposit ? Math.max(0, delta) : delta;\n      item.balance = Math.max(0, item.balance + effectiveDelta);\n      adjustCashOrFail(state, effectiveDelta, events);\n      if (meta.behaviour.isTermDeposit && effectiveDelta > 0) { const tenor=Math.max(6,state.behaviour.termDepositTenorMonths??12); genericFundingBuckets(state,item.productType).push({tenorMonths:tenor,monthsToMaturity:tenor,notional:effectiveDelta,rate:item.interestRate}); }", count=1)
# loan benchmark consumer
rep(p, "        meta.behaviour.loanBenchmark === 'mortgage'\n          ? state.market.competitorMortgageRate\n          : state.market.riskFreeLong + state.market.corporateLoanSpread;", "        meta.behaviour.loanBenchmark === 'mortgage' ? state.market.competitorMortgageRate : meta.behaviour.loanBenchmark === 'consumer' ? state.market.competitorConsumerLoanRate : state.market.riskFreeLong + state.market.corporateLoanSpread;")
# LTV modifies demand
rep(p, "        const demand =\n          neutralBankOpportunity * pipelineParams.baseDemandRateMonthly * dtMonths * pricingCapture;", "        const ltvDemandMultiplier = productType === AssetProductType.Mortgages ? clamp(1 + ((state.behaviour.mortgagePolicy?.maxLtv ?? .85) - .85) * 1.5, .75, 1.2) : 1;\n        const demand = neutralBankOpportunity * pipelineParams.baseDemandRateMonthly * dtMonths * pricingCapture * ltvDemandMultiplier;")
# call contractual maturity & treasury policy in engine step after actions
rep(p, "    applyActions(state, activeConfig, actions, events);\n    stepCompetitorReaction", "    applyActions(state, activeConfig, actions, events);\n    stepContractualRetailFunding(state, activeConfig, dtMonths, events);\n    applyTreasuryPolicy(state, activeConfig, events);\n    stepCompetitorReaction")

# ---- App form wiring ------------------------------------------------------
p='src/App.tsx'
# initial form additions
rep(p, "    retailDepositRate: formatRateInputPct(getGroupDepositRate(bankState, 'retail')),\n", "    retailDepositRate: formatRateInputPct(bankState.financial.balanceSheet.items.find(i=>i.productType===LiabilityProductType.RetailSavingsDeposits)?.interestRate ?? bankState.market.competitorRetailDepositRate),\n    termDepositRate: formatRateInputPct(bankState.financial.balanceSheet.items.find(i=>i.productType===LiabilityProductType.RetailTermDeposits)?.interestRate ?? bankState.market.competitorTermDepositRate),\n    termDepositTenorMonths: String(bankState.behaviour.termDepositTenorMonths ?? 12),\n")
rep(p, "    corporateLoanRate: formatRateInputPct(\n", "    consumerLoanRate: formatRateInputPct(bankState.financial.balanceSheet.items.find(i=>i.productType===AssetProductType.ConsumerLoans)?.interestRate ?? bankState.market.competitorConsumerLoanRate),\n    corporateLoanRate: formatRateInputPct(\n", count=1)
rep(p, "    corporateUnderwritingTightness:\n", "    consumerUnderwritingTightness: (bankState.behaviour.underwritingTightness?.[AssetProductType.ConsumerLoans] ?? 0.35).toString(),\n    corporateUnderwritingTightness:\n", count=1)
rep(p, "    issueLTDebtAmount: '',\n    issueEquityAmount: '',\n", "    mortgageMaxLtv: String(bankState.behaviour.mortgagePolicy?.maxLtv ?? .85),\n    mortgageFixedPeriodMonths: String(bankState.behaviour.mortgagePolicy?.fixedPeriodMonths ?? 24),\n    issueLTDebtAmount: '',\n    issueEquityAmount: '',\n    issueTier2Amount: '',\n", count=1)
rep(p, "    hedgeDirection: 'none',\n", "    giltShareOfHqla: String(bankState.behaviour.treasuryPolicy?.giltShareOfHqla ?? .625),\n    giltDurationYears: String(bankState.behaviour.treasuryPolicy?.giltDurationYears ?? 5),\n    boeFacility: 'none',\n    boeFundingAmount: '',\n    hedgeDirection: 'none',\n", count=1)
# scenario reset form easiest: add missing fields after its retail line and other markers (second occurrences)
text=read(p)
occ=[m.start() for m in re.finditer("      retailDepositRate: formatRateInputPct\(getGroupDepositRate\(scenarioState, 'retail'\)\),",text)]
if occ:
    text=text.replace("      retailDepositRate: formatRateInputPct(getGroupDepositRate(scenarioState, 'retail')),", "      retailDepositRate: formatRateInputPct(scenarioState.financial.balanceSheet.items.find(i=>i.productType===LiabilityProductType.RetailSavingsDeposits)?.interestRate ?? scenarioState.market.competitorRetailDepositRate),\n      termDepositRate: formatRateInputPct(scenarioState.financial.balanceSheet.items.find(i=>i.productType===LiabilityProductType.RetailTermDeposits)?.interestRate ?? scenarioState.market.competitorTermDepositRate),\n      termDepositTenorMonths: String(scenarioState.behaviour.termDepositTenorMonths ?? 12),",1)
write(p,text)
# patch scenario form around corporate loan, underwriting, transaction fields by global occurrence after scenario marker using direct generic replacements if still one expected
rep(p, "      corporateLoanRate: formatRateInputPct(\n", "      consumerLoanRate: formatRateInputPct(scenarioState.financial.balanceSheet.items.find(i=>i.productType===AssetProductType.ConsumerLoans)?.interestRate ?? scenarioState.market.competitorConsumerLoanRate),\n      corporateLoanRate: formatRateInputPct(\n", count=1)
rep(p, "      corporateUnderwritingTightness:\n", "      consumerUnderwritingTightness: (scenarioState.behaviour.underwritingTightness?.[AssetProductType.ConsumerLoans] ?? 0.35).toString(),\n      corporateUnderwritingTightness:\n", count=1)
rep(p, "      issueLTDebtAmount: '',\n      issueEquityAmount: '',\n", "      mortgageMaxLtv: String(scenarioState.behaviour.mortgagePolicy?.maxLtv ?? .85),\n      mortgageFixedPeriodMonths: String(scenarioState.behaviour.mortgagePolicy?.fixedPeriodMonths ?? 24),\n      issueLTDebtAmount: '',\n      issueEquityAmount: '',\n      issueTier2Amount: '',\n", count=1)
rep(p, "      hedgeDirection: 'none',\n", "      giltShareOfHqla: String(scenarioState.behaviour.treasuryPolicy?.giltShareOfHqla ?? .625),\n      giltDurationYears: String(scenarioState.behaviour.treasuryPolicy?.giltDurationYears ?? 5),\n      boeFacility: 'none',\n      boeFundingAmount: '',\n      hedgeDirection: 'none',\n", count=1)
# parse rate/underwriting/amount fields
rep(p, "    'retailDepositRate',\n    'corporateDepositRate',\n    'mortgageRate',\n    'corporateLoanRate',\n", "    'retailDepositRate', 'termDepositRate', 'corporateDepositRate', 'mortgageRate', 'consumerLoanRate', 'corporateLoanRate',\n")
rep(p, "    'mortgageUnderwritingTightness',\n    'corporateUnderwritingTightness',\n", "    'mortgageUnderwritingTightness', 'consumerUnderwritingTightness', 'corporateUnderwritingTightness',\n")
rep(p, "  const amountFields: Array<keyof ActionFormState> = ['issueLTDebtAmount', 'issueEquityAmount', 'hedgeNotional'];", "  const amountFields: Array<keyof ActionFormState> = ['issueLTDebtAmount','issueEquityAmount','issueTier2Amount','boeFundingAmount','hedgeNotional'];")
# additional simple numeric policy fields before payout parse
rep(p, "  const payoutParsed = parseRateInput(state.dividendPayoutRatio);", "  for (const field of ['mortgageMaxLtv','giltShareOfHqla'] as Array<keyof ActionFormState>) { const parsed=parseRateInput(state[field]); if(parsed.error) errors[field]=parsed.error; else if(parsed.value!==undefined){ if(parsed.value<0||parsed.value>1) errors[field]='Must be between 0 and 1'; else values[field]=parsed.value; } }\n  for (const field of ['mortgageFixedPeriodMonths','termDepositTenorMonths','giltDurationYears'] as Array<keyof ActionFormState>) { const raw=Number(state[field]); if(!Number.isFinite(raw)||raw<=0) errors[field]='Must be a positive number'; else values[field]=raw; }\n  if(state.boeFacility!=='none' && (values.boeFundingAmount??0)<=0) errors.boeFundingAmount='Enter an amount for the selected Bank of England facility';\n\n  const payoutParsed = parseRateInput(state.dividendPayoutRatio);")
# actions: retail savings only, term, consumer, new policy/actions
rep(p, "    [LiabilityProductType.RetailTransactionalDeposits, LiabilityProductType.RetailSavingsDeposits].forEach(\n      (productType) => {", "    [LiabilityProductType.RetailSavingsDeposits].forEach(\n      (productType) => {")
rep(p, "  if (values.corporateDepositRate !== undefined) {", "  if (values.termDepositRate !== undefined) actions.push({type:'adjustRate',productType:LiabilityProductType.RetailTermDeposits,newRate:values.termDepositRate});\n  if (values.termDepositTenorMonths !== undefined) actions.push({type:'setTermDepositPolicy',tenorMonths:values.termDepositTenorMonths});\n  if (values.corporateDepositRate !== undefined) {")
rep(p, "  if (values.corporateLoanRate !== undefined) {", "  if (values.consumerLoanRate !== undefined) actions.push({type:'adjustRate',productType:AssetProductType.ConsumerLoans,newRate:values.consumerLoanRate});\n  if (values.corporateLoanRate !== undefined) {")
rep(p, "  if (values.corporateUnderwritingTightness !== undefined) {", "  if (values.consumerUnderwritingTightness !== undefined) actions.push({type:'setUnderwriting',productType:AssetProductType.ConsumerLoans,tightness:values.consumerUnderwritingTightness});\n  if (values.corporateUnderwritingTightness !== undefined) {")
rep(p, "  if (values.issueLTDebtAmount !== undefined && values.issueLTDebtAmount > 0) {", "  if (values.mortgageMaxLtv!==undefined && values.mortgageFixedPeriodMonths!==undefined) actions.push({type:'setMortgagePolicy',maxLtv:values.mortgageMaxLtv,fixedPeriodMonths:values.mortgageFixedPeriodMonths});\n  if (values.giltShareOfHqla!==undefined && values.giltDurationYears!==undefined) actions.push({type:'setTreasuryPolicy',giltShareOfHqla:values.giltShareOfHqla,giltDurationYears:values.giltDurationYears});\n  if (state.boeFacility!=='none' && values.boeFundingAmount!==undefined && values.boeFundingAmount>0) actions.push({type:'drawBoeFunding',facility:state.boeFacility,amount:values.boeFundingAmount});\n  if (values.issueTier2Amount!==undefined && values.issueTier2Amount>0) actions.push({type:'issueTier2',amount:values.issueTier2Amount,maturityMonths:60});\n  if (values.issueLTDebtAmount !== undefined && values.issueLTDebtAmount > 0) {")

# ---- CSS compact strategy rows ------------------------------------------
p='src/styles.css'
text=read(p)
text += """\n/* Small-retail-bank decision hierarchy */\n.department-policy-panel{gap:.8rem}.policy-fields-primary{grid-template-columns:repeat(2,minmax(0,1fr))}.policy-disclosure{border-top:1px solid var(--border);padding-top:.55rem}.policy-disclosure>summary{font-weight:700;cursor:pointer}.loan-policy-grid{display:grid;gap:.55rem}.loan-policy-row{display:grid;grid-template-columns:minmax(150px,.9fr) repeat(2,minmax(150px,1fr));gap:.65rem;align-items:end;padding:.65rem 0;border-bottom:1px solid var(--border)}.loan-policy-row>div:first-child{align-self:center}.loan-policy-row>div:first-child small{display:block;margin-top:.15rem}.loan-policy-row .field{margin:0}@media(max-width:800px){.policy-fields-primary,.loan-policy-row{grid-template-columns:1fr}.loan-policy-row{align-items:stretch}}\n"""
write(p,text)

# marker prevents rerun from applying replacements twice
Path('.github/scripts/.retail_refocus_applied').write_text('1\n')
print('Retail-bank refocus patch applied')
